// You will use touples only two values, more bhi ker skhty hai but it is good for like key value pair

//1, "Taimoor" // so kai ilwa mai chata oor add na hoo

let users: [number, string] = [1, "Taimoor"];
// let users: [number, string] = [1, "Taimoor", 10]; //!ERROR

// users[0]. us kai mutabik suggestions
users.push(1);
