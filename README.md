Typescript:
    npm i -g typescript
    tsc -v

tsc fileName.ts

    tsc --init

tsc // to run all typescript files

If You Debug so you will use red point in your code 
now go to debug and create a launch.json and select node.js

f5 is a shortcut

run js files;

node dist/fileName.js